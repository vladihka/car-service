/**
 * Config Module
 * 
 * This module exports all configuration values.
 * All services should import from here, never from process.env directly.
 */

export { config, default as default } from './env';
